import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    // mode: 'dark',
    background: {
      paper: '#fff',
    },
    text: {
      primary: '#173A5E',
      secondary: '#46505A',
    },
    action: {
      active: '#001E3C',
    },
    success: {
      main: '#009688',
      dark: '#009688',
    },
  },
});
